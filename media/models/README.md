# models/

3D models render REAL here: .glb/.gltf and .3mf keep full textures/color. Parted models can EXPLODE.

.stl files also go here (they're 3D-printer files) — STL carries no color data at all, so it always renders as the blue hologram wire instead, no matter which folder it's in.
